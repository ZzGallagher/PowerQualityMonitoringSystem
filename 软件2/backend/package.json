{
  "name": "pq-monitor-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Software2 backend for software1 packet ingest and PostgreSQL-backed APIs.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "init-db": "node src/init-db.js",
    "check": "node --check src/server.js && node --check src/db.js && node --check src/ingest.js && node --check src/init-db.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "pg": "^8.13.1"
  }
}
