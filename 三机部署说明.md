# 电能质量监测系统三机部署说明

本项目正式演示架构拆成三部分：

```text
软件1采集打包端 -> 软件2后台服务 -> PostgreSQL数据库服务
软件2前端页面   -> 软件2后台服务 -> PostgreSQL数据库服务
```

软件1和软件2前端都不直接连接数据库。

## 1. 数据库服务器

安装 PostgreSQL，创建数据库和用户：

```sql
CREATE DATABASE pq_monitor;
CREATE USER pq_app WITH PASSWORD 'change_me';
GRANT ALL PRIVILEGES ON DATABASE pq_monitor TO pq_app;
```

数据库只需要向软件2后台所在机器开放 PostgreSQL 端口。

## 2. 软件2服务器

进入后台目录：

```powershell
cd "G:\#.Project\电能质量监测系统轻量版\软件2\backend"
```

安装依赖：

```powershell
npm install
```

复制并修改配置：

```powershell
Copy-Item .env.example .env
```

`.env` 示例：

```text
HOST=0.0.0.0
PORT=8000
DATABASE_URL=postgres://pq_app:change_me@数据库服务器IP:5432/pq_monitor
INGEST_TOKEN=
CORS_ORIGIN=*
```

初始化数据库：

```powershell
npm run init-db
```

启动后台：

```powershell
npm start
```

健康检查：

```powershell
Invoke-RestMethod -Uri "http://软件2服务器IP:8000/api/ingest/health" -Method Get
```

软件2前端配置文件：

```text
软件2/web/topology-config.js
```

将 `apiBaseUrl` 改为：

```js
apiBaseUrl: "http://软件2服务器IP:8000"
```

前端可以用任意静态服务托管，例如：

```powershell
cd "G:\#.Project\电能质量监测系统轻量版\软件2\web"
python -m http.server 8080
```

## 3. 软件1采集机器

修改软件1配置：

```text
软件1/amc_gateway/meter_config.example.json
```

接收端配置：

```json
{
  "receiver": {
    "baseUrl": "http://软件2服务器IP:8000",
    "ingestPath": "/api/ingest/packets",
    "healthPath": "/api/ingest/health",
    "testPath": "/api/ingest/test",
    "token": ""
  }
}
```

启动软件1：

```powershell
cd "G:\#.Project\电能质量监测系统轻量版\软件1\amc_gateway"
$env:PYTHONPATH=".\src"
python -m amc_gateway run --config meter_config.example.json --mode serial
```

无真实仪表时可先用：

```powershell
python -m amc_gateway run --config meter_config.example.json --mode mock
```

## 4. 验收检查

- 软件1启动后，软件2后台 `/api/ingest/packets` 返回成功。
- 软件2后台 `/api/realtime` 能看到 `amc-001` 的实时点位。
- 软件2前端弹窗中的电压、电流、功率来自后台 `/api/realtime`。
- `/api/interfaces/status` 能看到网关和仪表最近通信状态。
- 重复发送同一数据包不会重复写入历史采样。
